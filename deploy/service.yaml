apiVersion: v1
kind: Service
metadata:
  name: gh-upload
  namespace: ops-system
  labels:
    app: gh-upload
spec:
  type: ClusterIP
  selector:
    app: gh-upload
  ports:
    - name: http
      port: 80
      targetPort: http
