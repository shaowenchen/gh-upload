apiVersion: apps/v1
kind: Deployment
metadata:
  name: gh-upload
  namespace: ops-system
  labels:
    app: gh-upload
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gh-upload
  template:
    metadata:
      labels:
        app: gh-upload
    spec:
      containers:
        - name: gh-upload
          image: docker.io/shaowenchen/gh-upload:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: HOST
              value: "0.0.0.0"
            # Access token for writes. Empty means no restriction: uploads are
            # accepted without a credential. Set it to require one, e.g.:
            #   kubectl -n ops-system create secret generic gh-upload \
            #     --from-literal=admin-token="$(openssl rand -hex 32)"
            # then use valueFrom.secretKeyRef instead of a literal below.
            - name: ADMIN_TOKEN
              value: ""
            - name: GITHUB_TOKEN
              value: ""
            - name: GITHUB_REPO
              value: "uploadbases/cdn0"
            - name: GITHUB_BRANCH
              value: "raw"
            - name: GITHUB_COMMIT_EMAIL
              value: "auto@auto.com"
            - name: GITHUB_COMMIT_NAME
              value: "none"
            # Signing secret for download links. Empty = links never expire.
            #   kubectl -n ops-system create secret generic gh-upload \
            #     --from-literal=download-secret="$(openssl rand -hex 32)"
            - name: DOWNLOAD_SECRET
              value: ""
            - name: DOWNLOAD_TTL
              value: "604800"
            - name: DOWNLOAD_URLS
              value: ""
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 500m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1024Mi
